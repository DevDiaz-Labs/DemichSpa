---
// Services.astro
const services = [
  {
    id: 1,
    title: 'Masajes Relajantes',
    description: 'Libera la tensión de tu cuerpo y mente. Terapias diseñadas para brindarte beneficios inmensos y paz profunda.',
    image: 'https://images.unsplash.com/photo-1519823551278-64ac92734fb1?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80',
    link: '#contact'
  },
  {
    id: 2,
    title: 'Masajes Linfáticos Post Operatorios',
    description: 'Tratamientos especializados para reducir la inflamación, mejorar la circulación y acelerar tu recuperación tras una cirugía.',
    image: 'https://images.unsplash.com/photo-1600334129128-685c5582fd35?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80',
    link: '#contact'
  },
  {
    id: 3,
    title: 'Reductivos',
    description: 'Técnicas avanzadas para moldear tu figura, reducir medidas y tonificar tu cuerpo de manera efectiva y segura.',
    image: 'https://images.unsplash.com/photo-1544161515-4ab6ce6db874?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80',
    link: '#contact'
  },
  {
    id: 4,
    title: 'Faciales',
    description: 'Rejuvenece tu piel con un facial personalizado a tus necesidades específicas. Luce y siéntete radiante.',
    image: 'https://images.unsplash.com/photo-1515377905703-c4788e51af15?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80',
    link: '#contact'
  }
];
---

<section id="services" class="section services">
  <div class="container">
    <div class="section-header">
      <h2>Nuestros Servicios</h2>
      <div class="gold-separator mx-auto"></div>
      <p class="subtitle">En Spa Demich, te aseguramos cubrir todas tus necesidades de belleza y bienestar con los mejores productos y terapeutas especialistas.</p>
    </div>

    <div class="carousel-wrapper" id="services-carousel">
      <button class="carousel-btn prev-btn" aria-label="Anterior">&larr;</button>
      
      <div class="services-grid track">
        {services.map(service => (
          <div class="service-card item">
            <div class="card-image-wrapper">
              <img src={service.image} alt={service.title} class="card-image" />
            </div>
            <div class="card-content">
              <h3>{service.title}</h3>
              <p>{service.description}</p>
              <a href={service.link} class="card-link">Reservar &rarr;</a>
            </div>
          </div>
        ))}
      </div>

      <button class="carousel-btn next-btn" aria-label="Siguiente">&rarr;</button>
    </div>

    <div class="actions">
      <a href="#contact" class="btn btn-outline">Ver Todos los Servicios</a>
    </div>
  </div>
</section>

<style>
  .services {
    /* Uses global pure background */
    background-color: transparent; 
  }

  .section-header {
    text-align: center;
    max-width: 700px;
    margin: 0 auto 3rem auto;
  }

  .gold-separator {
    height: 2px;
    width: 60px;
    background-color: var(--c-primary);
    margin: 1rem auto 1.5rem auto;
  }

  .subtitle {
    font-size: 1.125rem;
    color: var(--c-text-light);
  }

  .carousel-wrapper {
    position: relative;
    margin-bottom: 4rem;
    min-width: 0; /* Fix flex/grid blowout */
    width: 100%;
  }

  .carousel-btn {
    display: none; /* Hidden on desktop by default as grid is used */
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
  }

  .service-card {
    position: relative;
    border-radius: 0;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    box-shadow: var(--shadow-sm);
    transition: transform var(--transition), box-shadow var(--transition);
  }

  .service-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-lg);
  }

  .card-image-wrapper {
    width: 100%;
    height: 300px;
    overflow: hidden;
  }

  .card-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
  }

  .service-card:hover .card-image {
    transform: scale(1.05);
  }

  .card-content {
    background-color: var(--c-bg-soft);
    padding: 1.5rem;
    position: relative;
    z-index: 2;
    margin: -3rem 1rem 0 0; /* Overlaps image on the left side but leaves gap on right */
    box-shadow: var(--shadow-sm);
    min-height: 220px;
    display: flex;
    flex-direction: column;
    border-top: 3px solid var(--c-primary);
    border-radius: 0 0 8px 8px;
  }

  .card-content h3 {
    font-size: 1.25rem;
    margin-bottom: 0.75rem;
  }

  .card-content p {
    font-size: 0.9rem;
    margin-bottom: 1.5rem;
    flex-grow: 1;
  }

  .card-link {
    font-weight: 600;
    color: var(--c-primary);
    text-transform: uppercase;
    font-size: 0.85rem;
    letter-spacing: 0.5px;
    display: flex;
    align-items: center;
  }

  .card-link:hover {
    color: var(--c-primary-hover);
    text-decoration: underline;
  }

  .actions {
    text-align: center;
    margin-top: 2rem;
  }

  .btn-outline {
    background-color: transparent;
    color: var(--c-primary);
    border: 2px solid var(--c-primary);
  }

  .btn-outline:hover {
    background-color: var(--c-primary);
    color: white;
  }

  @media (max-width: 1200px) {
    .services-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .carousel-wrapper {
      position: relative;
      margin-bottom: 2rem;
    }
    
    .services-grid {
      display: flex;
      flex-wrap: nowrap;
      overflow-x: auto;
      scroll-snap-type: x mandatory;
      scroll-behavior: smooth;
      gap: 1rem;
      padding-bottom: 2rem; /* space for scrollbar/shadows */
      -webkit-overflow-scrolling: touch;
      /* Hide scrollbar logically but allow scrolling */
      scrollbar-width: none; 
    }
    
    .services-grid::-webkit-scrollbar {
      display: none;
    }

    .service-card {
      flex: 0 0 85%;
      scroll-snap-align: center;
    }

    .card-content {
      margin: -2rem 1rem 0 1rem;
    }

    .carousel-btn {
      display: flex;
      align-items: center;
      justify-content: center;
      position: absolute;
      top: 40%;
      transform: translateY(-50%);
      width: 44px;
      height: 44px;
      border-radius: 50%;
      background: #fff;
      color: var(--c-primary);
      border: 1px solid rgba(212, 175, 55, 0.3);
      z-index: 10;
      box-shadow: var(--shadow-md);
      cursor: pointer;
      font-size: 1.25rem;
      transition: all 0.3s ease;
    }
    .carousel-btn:hover { 
      background: var(--c-primary); 
      color: #fff;
      box-shadow: var(--shadow-lg);
    }
    .prev-btn { left: -10px; }
    .next-btn { right: -10px; }
  }
</style>

<script>
  // Simple infinite carousel script for Services
  const container = document.getElementById('services-carousel');
  if (container) {
    const track = container.querySelector('.track');
    const prevBtn = container.querySelector('.prev-btn');
    const nextBtn = container.querySelector('.next-btn');

    if (track && prevBtn && nextBtn) {
      prevBtn.addEventListener('click', () => {
        const item = track.querySelector('.item');
        if (item) {
          const gap = 16;
          if (track.scrollLeft <= 10) {
            track.scrollTo({ left: track.scrollWidth, behavior: 'smooth' });
          } else {
            track.scrollBy({ left: -(item.clientWidth + gap), behavior: 'smooth' });
          }
        }
      });
      nextBtn.addEventListener('click', () => {
        const item = track.querySelector('.item');
        if (item) {
          const gap = 16;
          if (track.scrollLeft + track.clientWidth >= track.scrollWidth - 10) {
            track.scrollTo({ left: 0, behavior: 'smooth' });
          } else {
            track.scrollBy({ left: (item.clientWidth + gap), behavior: 'smooth' });
          }
        }
      });
    }
  }
</script>
